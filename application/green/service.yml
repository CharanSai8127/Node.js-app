apiVersion: v1
kind: Service
metadata:
  name: app-service-green
  labels:
    app: app
    version: green
spec:
  type: ClusterIP
  selector:
    app: app
    version: green
  ports:
  - name: http
    port: 80
    targetPort: 5000
