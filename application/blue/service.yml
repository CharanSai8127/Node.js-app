apiVersion: v1
kind: Service
metadata:
  name: app-service-blue
  labels:
    app: app
    version: blue
spec:
  type: ClusterIP
  selector:
    app: app
    version: blue
  ports:
  - name: http
    port: 80
    targetPort: 5000
